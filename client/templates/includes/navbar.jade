template(name="navbar")
  nav.navbar.navbar-default.navbar-fixed-top
    .container
      // Brand and toggle get grouped for better mobile display
      .navbar-header
        button.navbar-toggle.collapsed(type="button" data-toggle="collapse" data-target="#collapse-1")
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href="/") [logo]
      #collapse-1.collapse.navbar-collapse
        ul.nav.navbar-nav.navbar-right
          script.
            insertCircle({href: '/', fa: 'home', color: colors.home});
            insertCircle({href: '#', fa: 'flash', color: colors.live});
            insertCircle({href: '#', fa: 'gavel', color: colors.debate});
            insertCircle({href: '#', fa: 'music', color: colors.band});
            insertCircle({href: '#', fa: 'futbol-o', color: colors.sports});
            insertCircle({href: '#', fa: 'leaf', color: colors.clara});
            insertCircle({href: '#', fa: 'reorder', color: colors.events});
            insertCircle({href: '#', fa: 'calendar-o', color: colors.calendar});
            insertCircle({href: '#', fa: 'image', color: colors.gallery});
            insertCircle({href: '#', fa: 'pencil', color: colors.writers});
            insertCircle({href: '#', fa: 'users', color: colors.staff});
